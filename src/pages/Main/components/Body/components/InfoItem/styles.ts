import { createStyles } from '@mantine/core';

export const useStyles = createStyles(() => ({
  Wrapper: {
    display: 'flex',
    flexDirection: 'column'
  },
  Title: {
    fontSize: '1.15rem'
  },
  Value: {
    fontSize: '2.5rem'
  }
}));
