import { createStyles } from '@mantine/core';

export const useStyles = createStyles(() => ({
  Wrapper: {
    width: '100%',
    display: 'flex',
    justifyContent: 'center',
    marginTop: '8rem',
    fontWeight: 'bold'
  }
}));
